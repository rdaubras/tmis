# Server-only: the Next.js server (route handlers / server actions /
# server components) talks to the TMIS API from here. Never exposed to
# the browser — the frontend never holds a bearer token or calls the API
# directly from client JS (see src/lib/session.ts, src/lib/api.ts).
TMIS_API_BASE_URL=http://localhost:8000
