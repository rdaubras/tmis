import { ModulePlaceholder } from "@/components/module-placeholder";

export default function ResearchPage() {
  return (
    <ModulePlaceholder
      title="Recherche documentaire"
      description="Recherche hybride sur les sources juridiques configurées (codes, jurisprudence, doctrine)."
      sprint={8}
    />
  );
}
