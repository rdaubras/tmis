import { ModulePlaceholder } from "@/components/module-placeholder";

export default function AdminPage() {
  return (
    <ModulePlaceholder
      title="Administration"
      description="Supervision multi-tenant, audit, feature flags, configuration des connecteurs."
      sprint={23}
    />
  );
}
