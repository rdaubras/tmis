"""Cross-cutting data contracts shared by every Document Intelligence
module. Nothing here depends on another `document_intelligence.*` module —
it is the base layer everything else is built on (same pattern as
`tmis.ai.schemas`, see docs/10-ai-kernel.md).
"""
