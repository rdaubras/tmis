"""TMIS AI Kernel: the single home for every AI capability in the platform.

No other package talks to a model provider, a document connector, or an
external memory/cache backend directly — everything goes through
`tmis.ai.kernel.TMISKernel`. See docs/10-ai-kernel.md.
"""
