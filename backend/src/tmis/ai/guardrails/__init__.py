"""Guardrails: input/output validation and error handling shared by every
AI Kernel call.

Business rules (legal-specific checks) are added in future sprints; this
package only ships the generic mechanics (see docs/09-roadmap-30-sprints.md).
"""
