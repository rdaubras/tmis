"""Tool calling: functions an agent can invoke through the Kernel.

No business tool ships in Sprint 2 (see docs/09-roadmap-30-sprints.md);
`current_datetime_tool.py` only proves the registration mechanism works.
"""
