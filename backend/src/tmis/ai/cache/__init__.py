"""Cache abstraction used for costly model calls and repeated searches.

See `CachePort` for the interface; `InMemoryCache` is the default/test
implementation, `RedisCache` is the production implementation.
"""
