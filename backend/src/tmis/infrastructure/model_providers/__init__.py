"""Interchangeable adapters implementing `tmis.domain.shared.ports.ModelProviderPort`.

Selecting a provider is a configuration concern (see `tmis.core.config`),
never a code change in the domain or application layers.
"""
